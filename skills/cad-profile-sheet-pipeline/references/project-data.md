# 프로젝트 데이터: 고색처리분구 종평면도

좌표를 재사용하기 전에는 현재 열린 DWG가 아래 대상과 같은지 확인한다.

## 문서와 도곽

- 대상: `C1-024~068 고색 오수관로정비 종·평면도(1)~(45)RE2_주상도표기 r1.dwg`
- 관망도: `고색관망도.dwg`
- Frame 원점: `X0 = 209505.7 + 420.5*((N-1) mod 10)`, `Y0 = 512722.1 - 297.0*floor((N-1)/10)`
- 도곽 크기: 420.5 × 297.0
- 평면 박스: `(X0+38.34,Y0+203.25)` ~ `(X0+282.94,Y0+258.25)`
- 종단면 기준점: `(X0+37.9,Y0+16.5)`

## Frame 11 검증값

- 도면번호/매수: `C1-034`, `11/45`
- 노선: `GS-129-001~009`
- 평면 박스: `(209544.04,512628.35)` ~ `(209788.64,512683.35)`
- 관망도 XREF: 기존 `고색관망도` 정의 재사용 (`reuseExistingDefinition: true`)
- 삽입점: `(633640.404858145,156507.230294564)`
- 회전: `70.9625418°`, 스케일 1.0, 레이어 `XREF-관망도`
- 키맵 타일 8 bbox: `(209869.135,512658.75)` ~ `(209873.095,512661.39)`, 레이어 `도엽-`
- 문자 스타일: `HY울릉도M`
- 주요 스냅샷: 종단면 `20260805-173905-5e5e964f`, XREF 교체 `20260805-181031-ff5db538`, 평면 주석 `20260805-181804-711aa278`
- 평면 주석 결과: 문자 18개, `신설 기호` 9개, 지시선 9개, `verified: true`, mismatch 없음

`Drawing1.dwg`의 GS-129 종단면 716개 객체를 대상 ModelSpace의 `Add*` 직접 COM 경로로 재생성했다. 관망도는 기존 `고색관망도` XREF 정의를 재사용해 완성 Frame과 같은 종속 레이어 동결 상태를 유지했고, 도형·문자·반복 블록은 ModelSpace에 직접 생성했으며 경계는 기본 XCLIP으로 처리했다. 연결 길이는 `19.6 → 5.5 → 30.0 → 23.5 → 8.6 → 24.5 → 14.5 → 17.7 → 68.0m` 순서로 검증했다.

## 규칙

- 관망도 중심선, 맨홀, 관로번호 문자를 함께 사용해 연결 순서를 판정한다.
- 가까운 점 하나만 골라 방향을 정하지 않는다.
- 인접 완성 Frame에서 문자 높이·정렬·레이어를 읽는다.
- 키맵은 해당 Frame의 한 칸만 표시하고 적용 후 bbox를 확인한다.
